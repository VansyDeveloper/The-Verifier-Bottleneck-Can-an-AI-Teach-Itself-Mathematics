# Окончательный снимок очередей на 24 сентября 2026 года

Тринадцать JSONL-файлов взяты с фактических серверов исполнения после `SET_DONE` каждого набора. Для каждого набора использован ровно один авторитетный журнал. SHA-256 каждой локальной копии повторно совпал с удалённым файлом. Прежние промежуточные снимки не использовались при построении этой матрицы.

`ccmplanner` содержит наборы 1/1, 1/2, 1/4, 2/2, 2/5, 3/1 и 3/3. `cdsserver` содержит 1/3, 1/5, 2/4 и 3/2. `cds2` содержит 2/1 и восстановленный 2/3. Исходные файлы на серверах не изменены.

`run_index.csv` построен скриптом `reports/withheld_pairs_20260923/build_run_index.py` с тринадцатью явными аргументами `--queue-log`. Он содержит 120 строк по замороженному дизайну. Из них 78 имеют локально принятые обучение и полный комплект оценок, 36 не начаты, шесть относятся к заранее признанному неосуществимым набору 5/5. Удалённые журналы также подтверждают 78 принятых обучений и 78 принятых оценок. События `SKIPPED_ACCEPTED` означают повторное использование уже принятых заданий после безопасного восстановления, а не дополнительное обучение.

SHA-256 `run_index.csv`: `9A2926FA44E6BB1B10201D037EF873C9CDAF2C489E195FDB7429822AFE5AC196`. Идентичная по хешу окончательная матрица лежит в `reports/withheld_pairs_20260923/data/run_index.csv`. Парное сравнение с бонусом энтропии учитывается отдельно в `reports/grpo_entropy_20260923/data/run_index_verified.csv`.
